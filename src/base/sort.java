package base;

import java.util.Arrays;
import java.util.PriorityQueue;

public class sort {
    public int[] solution(int[] numlist, int n) {

        int[] a = new int[numlist.length];

        for(int i=0; i < numlist.length; i++){
            a[i] = n - numlist[i];
        }

        // 1. n과 가장 가까운 정수찾기
        // 2. 앞 뒤중 어떤게 큰지 찾고 새로운 배열에 담기




        int[] answer = {};
        return answer;
    }
}
